import 'package:flutter/material.dart';

class DriversManagementView extends StatelessWidget {
  const DriversManagementView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Drivers Management'),
      ),
      body: const Center(
        child: Text('Drivers Management View - Coming Soon'),
      ),
    );
  }
}
