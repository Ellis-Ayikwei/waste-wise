import 'package:flutter/material.dart';

class ActiveJobsView extends StatelessWidget {
  const ActiveJobsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Active Jobs'),
      ),
      body: const Center(
        child: Text('Active Jobs View - Coming Soon'),
      ),
    );
  }
}
