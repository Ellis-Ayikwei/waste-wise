import 'package:flutter/material.dart';

class ImpactReportsView extends StatelessWidget {
  const ImpactReportsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Impact Reports'),
      ),
      body: const Center(
        child: Text('Impact Reports View - Coming Soon'),
      ),
    );
  }
}
